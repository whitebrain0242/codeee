```
┌─────────────────────────────────────────────────────────────────────┐
│                         客户端 (Client)                            │
├─────────────────────────────────────────────────────────────────────┤
│  ① 业务逻辑：发起上传请求                                           │
│     │                                                              │
│     ├── ② 【fileutil】生成传输令牌 (make_transfer_token)           │
│     ├── ③ 【fileutil】计算文件 SHA256 (sha256_file_hex)            │
│     └── ④ 【fileutil】清理文件名 (sanitize_filename)                │
│         │                                                          │
│         ▼                                                          │
│  ⑤ 网络层 (muduo)：调用 TcpConnection::send()                     │
│     │                                                              │
│     ▼                                                              │
│  ⑥ 【TLS 加密隧道】 (OpenSSL)                                      │
│     └── 将二进制流 + 元数据（Token/哈希/文件名）打包加密发送        │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │  ═══ 互联网 (加密传输) ═══
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      服务端 (Server)                                │
├─────────────────────────────────────────────────────────────────────┤
│  ⑦ 【TLS 解密隧道】 (OpenSSL) 还原原始字节流                       │
│     │                                                              │
│     ▼                                                              │
│  ⑧ 网络层 (muduo)：TcpConnection 收到数据，触发 messageCallback   │
│     │                                                              │
│     ▼                                                              │
│  ⑨ 【业务逻辑层 —— 你的 FileServer 类】                            │
│     │                                                              │
│     ├── ⑩ 【fileutil】校验传输令牌 (is_valid_transfer_token)       │
│     │        └── 无效则直接断开，防止非法上传                      │
│     │                                                              │
│     ├── ⑪ 【fileutil】清理文件名 (sanitize_filename)               │
│     │        └── 过滤掉 ../ 等危险字符，防止写系统目录             │
│     │                                                              │
│     ├── ⑫ 将文件二进制流写入磁盘 (fwrite / ofstream)              │
│     │                                                              │
│     ├── ⑬ 【fileutil】计算已存文件的 SHA256 (sha256_file_hex)      │
│     │        └── 得到服务端本地哈希值                              │
│     │                                                              │
│     └── ⑭ 比对客户端发来的哈希值 vs 服务端计算的哈希值            │
│              ├── 一致 → 保存记录，返回 200 OK                      │
│              └── 不一致 → 删除文件，返回 500 重传                  │
└─────────────────────────────────────────────────────────────────────┘

```
